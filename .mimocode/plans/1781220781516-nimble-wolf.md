# Plano: Corrigir Erro OAuth Google em Produção — ✅ RESOLVIDO

## Resumo do que foi feito no VPS

1. **Migration `pricingVersion`** — Commit `3b972af` + deploy. Migration foi aplicada mas ficou marcada como "failed" (P3009) no banco.
2. **Entrypoint** — `docker-compose.yml` alterado para saltar `prisma migrate deploy` (que batia no P3009).
3. **Caddyfile** — Adicionados blocos `handle /auth/*` e `handle /operations/*` para fazer proxy de POSTs para o servidor Wasp (antes o `file_server` rejeitava com 405).
4. **NODE_ENV=production** — Adicionado ao `docker-compose.yml` para corrigir hidratação React (#418).

## Resultado
✅ Login Google funcional
✅ Onboarding aparece para novos usuários
✅ Sidebar e workspace switcher carregam

---

## Tarefas pendentes (cleanup)

### 1. Atualizar Caddyfile no repo
**Arquivo**: `deploy/Caddyfile` (e `deploy/Caddyfile.homolog`)
O Caddyfile no repo está desatualizado (sem `handle /auth/*` e `handle /operations/*`). O próximo deploy vai sobrescrever a correção no VPS.

### 2. Resolver P3009 permanentemente
A migration `20260612000336_home` está marcada como "MigrationFailed" no `_prisma_migrations` do banco Neon. Precisamos:
- Conectar ao banco Neon e executar:
  ```sql
  DELETE FROM "_prisma_migrations" WHERE "migration_name" = '20260612000336_home';
  ```
  OU marcar como sucesso:
  ```sql
  UPDATE "_prisma_migrations" SET "finished_at" = NOW(), "applied_steps_count" = 1, "status" = 'MigrationSuccess', "logs" = NULL WHERE "migration_name" = '20260612000336_home';
  ```
- Depois remover o `entrypoint` customizado do `docker-compose.yml` para restaurar o comportamento normal.

### 3. Atualizar docker-compose.yml no repo
- Adicionar `NODE_ENV: production` ao serviço `server`
- Remover o `entrypoint` customizado (após resolver P3009)

### 4. Commit + deploy final
Após as correções acima, commit e deploy para homolog e produção.
